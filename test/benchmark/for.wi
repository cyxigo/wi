var start = os.clock();
var arr = [];

for (var i = 0; i < 1000000; i = i + 1) {
    arr->add(i);
}

var sum = 0;

for (var i = 0; i < #arr; i = i + 1) {
    sum = sum + arr[i];
}

print(sum);
print("elapsed: " .. os.clock() - start);
