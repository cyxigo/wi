var fib = function(n) {
    if (n < 2) {
        return n;
    }

    return fib(n - 1) + fib(n - 2);
};

var start = os.clock();

for (var i = 0; i < 5; i = i + 1) {
    print(fib(28));
}

print("elapsed: " .. os.clock() - start);
