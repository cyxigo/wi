fib := |n| => {
    if (n < 2) {
        return n;
    }

    return fib(n - 1) + fib(n - 2);
};

start := os.clock();

for (i := 0; i < 5; i = i + 1) {
    print(fib(28));
}

print("elapsed ${os.clock() - start}");
