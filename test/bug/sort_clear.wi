// $ok
// Bug in v8.2.0-beta, clear mid-sort causes segfault
arr := [0, 1, 2, 3, -2];
arr->sort(|a, b| => {
    arr->clear();
    return a < b;
});

// Expected:
// runtime error: array resized during sort
//    --> test/bug/sort_clear.wi:7 in main function
